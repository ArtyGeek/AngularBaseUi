(function() {
    'use strict';

    /**
     *
     * Should be created component module
     * and injected different modules (if need)
     *
     */

    angular.module('app.components.sample-component', []);
})();
